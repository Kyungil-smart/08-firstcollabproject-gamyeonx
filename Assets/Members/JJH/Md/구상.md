1. 생성하려는 건물과 프리펩 및 피봇 연결
- 이니시에잇 할건지 오브젝트 풀 패턴을 할건지 (성능문제관련)

- 오브젝트 풀 패턴했을때는 또 NPC랑 따로 매니저를 만들긴 해야할거같은데 (NPC Pool Manager / Map Pool Manager)

- 오브젝트 풀 패턴 안쓸때는 어떻게보면 건물 생성시마다 이니시에잇이 두번되어야하긴함 건물생성은 유저가 직접하는거니까 SetActive로는 구현힘들지않을까..(성능 문제 있을 수 있음)

- 매니저를 하나 두는건 거의 확정 (MapManager 라던지 Camera Manager라던지)

- 피봇과 프리펩을 연결(레벨마다도 연결) 해서 생성 시 카운트를 센뒤 카운트에 따라 피봇의 트랜스폼에서 멀어지는 방식으로 구현?

- 그럼 생성될때마다 프리펩과 생성된 내부구조물의 연결은 어떻게 할것인지? (카메라 이동이나 레벨업 관련 부분)

- 내부구조물마다 빈 오브젝트의 피봇을 만든다음에 생성시 생성구조물과 자동으로 연결?

- 이렇게되면 맵 뷰 변경시 매니저쪽에서도 해당 구조물이 어디랑 연결되어있는지 확인 후 해당구조물의 피봇의 트랜스폼을 알아야하는데 어떻게 할것인지?

- 그리고 Onclick이벤트도 피봇의 트랜스폼이나 레벨관련해서 생성될 때 자동 연결이 되어야할것인데.. (해당부분은 오브젝트풀사용하면 미리만들어두면되니까 해결될수도? 근데또 생성할때 Set부분에서 자동으로 매치되어야하는데.. 카운트에 따라서 연결할수 있는 방법이 있을지 고려)

- 간단하게 스크립트 하나 붙이고 생성될때 awake나 start 단계에서 public으로 자기 위치 기억하게 한뒤 맵매니저에서 불러와서 카메라 피봇을 변경한다?

- 근데 구조물마다도 스크립트가 있어야할거고 뭐 만족도,금액관련 내용은 있어야할것

- 그럼 프리펩이 여러개일텐데 총괄하는 빈오브젝트가 있긴 해야할듯(NPC가 하위 프리펩의 기능 이용시 금액상승 혹은 컨디션 상승 반영되어야 함)

- 1번프리펩 (최초 건물 생성시 1레벨에 기본적으로 생성되는 프리펩)에 만약 스크립트를 단다고하면 나머지 프리펩이 생성될때 해당 프리펩 하위에 들어가야할것같은데,..

- 해당부분은 이벤트로 할것인지?

- 일단 레벨업시에도 카운트를 세서 다음프리펩을 붙이는 방향으로 해야할듯 왠만하면 옵저버패턴 이용해서 이용금액 상승이나 이런거랑도 연계되어야 할것으로 보임

- 이러면 하나의 매니저가 많은일을 하게되는데 부하가능성은 없는지 고려

- 추후 추가되겠지만 Save/Load기능에서 기억해야할 부분을 정해야할텐데 ...



*** 일단 가장 쉬운방법은 첫번째 피봇에서 어느정도 떨어진 거리에 빈 오브젝트를 자동으로 생성하는 기능(스크립트도 자동으로 붙여져 있어야함)이 있으면 어느정도 문제점 해결 가능 ***
빈오브젝트를 프리펩화 한다고 했을때 차피 하이어라키에서는 자동으로 뒤에 (1) 이런식으로 붙을테니까 그거 이용할 방법 있는지 생각

많은 기능이 혼합되어야할것으로 보이는데 성능문제는 없으련지 고민


** 내일 우선적으로 해볼 꺼 정리

일단 빈오브젝트 프리펩 만들어서 하위에 버튼 2개 두고 유저가 생성 시 OnClick 이벤트 자동으로 연결되는 기능
유저가 생성 시 Pivot 위치에 건물 내부 프리펩도 생성되어야함
내부 프리펩과 Onclick 이벤트가 매칭되어야함
그뒤 생성 시 떨어진 위치에 자동 생성되어야함
레벨업 버튼 누를 시 다음 프리펩이 자동으로 생성되어야함

** 스크립트
총괄할 매니저 스크립트
버튼 이벤트 관련 스크립트 -> 근데 아 그럼 캔버스가 프리펩마다 하위에 들어가있어야하나..
건물 내부와 외부 연결해줄 스크립트
카메라 스크립트 수정 (내부로이동시 center 및 사이즈 변경하는 내용) -> 코루틴 사용 안하는게 나을듯 같은씬이다보니까 이동하다가 다른 건물 내부 보일 가능성 있음
디자인패턴 사용시 디자인패턴 관련 매니저 스크립트